made a couple of changes to the profile.tsx, profilemenu.tsx in the profile_section, also added in editprofilemodal, changepasswordmodal and deleteaccountmodal in the profile_section compoenents

dummy data was also changed to have logic checks on password

createacconutconsts.tsx was changed to allow for dummy user to be logged into data for signup, however it is only in-memory and not saved into dummy data itself

profileheader in profile_section also changed to pull the user instead of being hardcoded